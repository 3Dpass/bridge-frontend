# Use pnpm's strict peer dependencies
strict-peer-dependencies=false

# Auto-install peers
auto-install-peers=true

# Shamefully hoist (for React compatibility)
shamefully-hoist=true

# Use node-linker for better compatibility with tools expecting node_modules
node-linker=hoisted
